language: node_js
node_js:
  - 12

env:
  - DATABASE_URL=postgresql://codex:pg123@localhost:5432/greetings

before_script:
  - psql -c 'create database  greetings;' -U codex
  - psql -U codex -d greetings -a -f users.sql

services:
  - postgresql
