language: node_js
node_js:
  - 12

env:
  - DATABASE_URL=postgresql://postgres@localhost:5432/my_products_tests


before_script:
  - psql -c 'create database  greetings;' -U postgres
  - psql -U postgres -d greetings -a -f users.sql

services:
  - postgresql
